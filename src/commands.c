#include "commands.h"
#include <stdio.h>
#include <string.h>

void default_process(char** args, OUT_M1* commands)
{
  printf("Non-implemented command !!");
}


void start_test(char** args, OUT_M1* commands)
{
  byte i = 0;
/*
  if(args[i] != '\0')
  {
    while(args[i] != '\0')
    {
      if(strcmp(args[i], "NULL"))
      {
        commands->Etat_Epreuve = (enum Epreuve)values[i];
        i++;
      }
    }
  }
  else{
    commands->Etat_Epreuve = epreuve1;
  }
  */

}
